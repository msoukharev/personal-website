const { getJSON } = require("jquery");

import {getJSON} from jQuery

export function personal_info(url) {

    let request = getJSON(url)
    return request

}

export function github_project(username) {
    
}
